apiVersion: redhatcop.redhat.io/v1alpha1
kind: SecretEngineMount
metadata:
  name: test-rmq-mount
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  type: rabbitmq
  config:
    listingVisibility: "hidden"
  path: test-rmqse
