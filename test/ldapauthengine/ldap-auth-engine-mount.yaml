apiVersion: redhatcop.redhat.io/v1alpha1
kind: AuthEngineMount
metadata:
  name: test
spec:
  authentication: 
    path: kubernetes
    role: vault-admin
    serviceAccount:
      name: default
  type: ldap
  path: ldap