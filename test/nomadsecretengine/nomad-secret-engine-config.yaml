apiVersion: redhatcop.redhat.io/v1alpha1
kind: NomadSecretEngineConfig
metadata:
  name: nomad-config-test
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  path: test-nomadse/test-nomadse-mount
  address: "http://nomad.nomad.svc.cluster.local:4646"
  rootCredentials:
    secret:
      name: nomad-token-secret
    passwordKey: token
