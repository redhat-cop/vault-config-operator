apiVersion: redhatcop.redhat.io/v1alpha1
kind: AuthEngineMount
metadata:
  name: test-userpass-mount
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  type: userpass
  config:
    listingVisibility: "hidden"
  path: test-userpass-auth
