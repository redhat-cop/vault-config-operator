apiVersion: redhatcop.redhat.io/v1alpha1
kind: ConsulSecretEngineRole
metadata:
  name: consul-role-test
  namespace: test-vault-config-operator
spec:
  authentication:
    path: kubernetes
    role: vault-admin
  path: consul
  consulPolicies:
    - readonly
  ttl: "1h"
  maxTTL: "24h"
