apiVersion: redhatcop.redhat.io/v1alpha1
kind: SecretEngineMount
metadata:
  name: test-ldapse-mount
spec:
  authentication:
    path: kubernetes
    role: policy-admin
  type: openldap
  config:
    listingVisibility: "hidden"
  path: test-ldapse
