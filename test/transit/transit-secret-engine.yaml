apiVersion: redhatcop.redhat.io/v1alpha1
kind: SecretEngineMount
metadata:
  name: transit
spec:
  authentication:
    path: kubernetes
    role: transit-engine-auth-role
    serviceAccount:
      name: default
  type: transit
  path: test-vault-config-operator
  config:
    listingVisibility: hidden
