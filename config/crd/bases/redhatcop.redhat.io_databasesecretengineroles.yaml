
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: databasesecretengineroles.redhatcop.redhat.io
spec:
  group: redhatcop.redhat.io
  names:
    kind: DatabaseSecretEngineRole
    listKind: DatabaseSecretEngineRoleList
    plural: databasesecretengineroles
    singular: databasesecretenginerole
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatabaseSecretEngineRole is the Schema for the databasesecretengineroles
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseSecretEngineRoleSpec defines the desired state of
              DatabaseSecretEngineRole
            properties:
              authentication:
                description: Authentication is the kube aoth configuraiton to be used
                  to execute this request
                properties:
                  namespace:
                    description: Namespace is the Vault namespace to be used in all
                      the operations withing this connection/authentication. Only
                      available in Vault Enterprise.
                    type: string
                  path:
                    description: Path is the path of the role used for this kube auth
                      authentication
                    type: string
                  serviceAccount:
                    default:
                      Name: default
                    description: ServiceAccount is the service account used for the
                      kube auth authentication
                    properties:
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                    type: object
                type: object
              creationStatements:
                description: CreationStatements Specifies the database statements
                  executed to create and configure a user. See the plugin's API page
                  for more information on support and formatting for this parameter.
                items:
                  type: string
                type: array
                uniqueItems: true
                x-kubernetes-list-type: set
              dBName:
                description: DBName The name of the database connection to use for
                  this role.
                type: string
              defaultTTL:
                default: 0
                description: DeafulTTL Specifies the TTL for the leases associated
                  with this role. Accepts time suffixed strings ("1h") or an integer
                  number of seconds. Defaults to system/engine default TTL time.
                type: string
              maxTTL:
                default: 0
                description: MaxTTL Specifies the maximum TTL for the leases associated
                  with this role. Accepts time suffixed strings ("1h") or an integer
                  number of seconds. Defaults to system/mount default TTL time; this
                  value is allowed to be less than the mount max TTL (or, if not set,
                  the system max TTL), but it is not allowed to be longer. See also
                  The TTL General Case.
                type: string
              renewStatements:
                description: RenewStatements Specifies the database statements to
                  be executed to renew a user. Not every plugin type will support
                  this functionality. See the plugin's API page for more information
                  on support and formatting for this parameter.
                items:
                  type: string
                type: array
                uniqueItems: true
                x-kubernetes-list-type: set
              revocationStatements:
                description: RevocationStatements Specifies the database statements
                  to be executed to revoke a user. See the plugin's API page for more
                  information on support and formatting for this parameter.
                items:
                  type: string
                type: array
                uniqueItems: true
                x-kubernetes-list-type: set
              rollbackStatements:
                description: RollbackStatements Specifies the database statements
                  to be executed to rollback a create operation in the event of an
                  error. Not every plugin type will support this functionality. See
                  the plugin's API page for more information on support and formatting
                  for this parameter.
                items:
                  type: string
                type: array
                uniqueItems: true
                x-kubernetes-list-type: set
            type: object
          status:
            description: DatabaseSecretEngineRoleStatus defines the observed state
              of DatabaseSecretEngineRole
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
