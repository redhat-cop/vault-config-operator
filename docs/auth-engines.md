# Authentication Engines APIs

This documentation has moved to [docs/auth-engines/](auth-engines/index.md).

The auth engine documentation has been split into per-engine files for easier navigation. See the [auth engines index](auth-engines/index.md) for the full list of supported engines and links to each engine's documentation.
