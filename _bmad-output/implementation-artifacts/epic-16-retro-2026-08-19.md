# Epic 16 Retrospective — Lower-Priority Missing Auth Methods (RADIUS, AliCloud, OCI, Kerberos, CF)

**Date:** 2026-08-19
**Epic:** 16 — Lower-Priority Missing Auth Methods (RADIUS, AliCloud, OCI, Kerberos, CF)
**Stories:** 16.1, 16.2, 16.3, 16.4, 16.5

---

## Epic Summary

All five stories delivered at 100% completion. New CRDs: RADIUS Config+User, AliCloud Role (no config endpoint), OCI Config+Role, Kerberos Config+LDAP Config+Group, CF Config+Role. Documentation exists for all five engines (DNFR5). Integration tests were Skip for every story (external RADIUS / cloud / Kerberos infra); unit tests were the quality gate.

This retrospective executed leftover work in-session, same standing rule as Epic 15. RADIUS `token_type` false-drift is fixed. Phase B story-file finalization is now a script, not a documentation rule. The ENABLE_WEBHOOKS passwordKey residual is explicitly no-action (webhooks-on is the production contract). Propagating `token_type` read-shape emit to other always-emit types is deferred.

## What Went Well

1. **100% delivery:** 5/5 stories done. CRDs, docs, kustomize registration, main.go wiring.
2. **Epic 15 notes stuck:** Kerberos shipped as three CRDs (1:1 VaultObject). TTL used `durationToSeconds()` from the first commit. CF and RADIUS webhooks copied AWS omit-vs-explicit credential defaulting.
3. **16.2–16.5 closed review findings in-story:** AliCloud ARN parsing, OCI sparse-role drift, Kerberos group `policies` array shape and LDAP Secret watches, CF RandomSecret username and Vault read aliases. Not banked for a later retro.
4. **No shared-framework changes:** All work additive. CRD registration checklist followed.
5. **Integration-test classification honored:** Skip for all five, documented in story files.

## What Could Be Improved

1. **16.1 hit the 5/5 review cap.** Two findings were accepted-as-is. One was a real Vault-read-shape bug (`token_type: ""` vs `"default"`). The other was a local-dev env-var helper that is not a production issue if webhooks stay on.
2. **Review-cap leftover tests did not match Vault.** The sparse RADIUS equivalence test omitted `token_type` from the fake Vault payload, so it could not catch the `"default"` read shape documented in the same story.
3. **Phase B finalization was still a doc rule.** Carried from Epic 13 through 15. Documentation-only HARD GUARDs in `phase-logic.md` do not stop an orchestrator from hand-editing sprint-status.

## Key Decisions (This Session)

| # | Decision | Verdict | Rationale |
|---|----------|---------|-----------|
| 1 | Residual 1 (ENABLE_WEBHOOKS passwordKey remap) | No action | Webhooks-on is the production contract. The webhook already distinguishes omit vs explicit. The env-var helper only matters when `ENABLE_WEBHOOKS=false` (local-dev without cert-manager). Same gap AWS already accepts. |
| 2 | Residual 2 (omitted `tokenType` vs Vault `"default"`) | Fixed | Emit `"default"` when `TokenType` is empty so `toMap()` matches Vault's read shape (Kubernetes pattern). Do not omit-if-empty (GitHub/Userpass) — that fights iteration 4's always-emit stale-clearing. |
| 3 | Phase B story-file atomicity | Fixed as code | `finalize-story.py` is the only allowed way to mark a story `done` in sprint-status. Refuses without a Code Review Record. Sets story `Status:` and sprint-status together. `--check-epic` is the layer/resume gate. |
| 4 | Propagate `token_type` emit to other always-emit types | Deferred | Same bug class on Okta, AppRole, AliCloud, OCI, CF, LDAP, AWS, Azure, GCP, JWT, Cert, Kerberos. Out of scope for this retro. |
| 5 | No Epic 17 | Confirmed | Phase 2 FR map ends at AE6–AE10. This retro is reflection plus residual cleanup, not a kickoff. |

## Epic 15 Action Item Follow-Through

| # | Action | Status at start | This session |
|---|--------|-----------------|--------------|
| 1 | Fix Okta `toMap()` TTL via `durationToSeconds()` | Done | Held |
| 2 | Story 15.2 Status + Code Review Record | Done | Held |
| 3 | Annotate Epic 16 (TTL, Kerberos split, credential defaulting, novelty) | Done | **Applied** — Kerberos three CRDs, TTL from commit one, AWS-style defaulting on CF/RADIUS |
| 4 | Restore create-story template + Novelty Risk + TTL rule | Done | Held |
| 5 | Phase B story-file finalization in the orchestrator | Open | **Done** — `finalize-story.py` + tests + phase-logic / SKILL.md / toml wiring |

## Action Items Completed This Session

| # | Action | Owner | Result |
|---|--------|-------|--------|
| 1 | RADIUS empty `TokenType` emits `token_type: "default"`; tests use Vault's real read shape | Amelia (Developer) | Done — `go test ./api/v1alpha1/ -run RADIUS` pass |
| 2 | ENABLE_WEBHOOKS passwordKey remap | Amelia (Developer) | No action |
| 3 | `finalize-story.py` atomic story+sprint-status gate; `--check-epic`; unit tests; wired into epic-dev | Bob (Scrum Master) | Done — 11 tests pass; `--check-epic 16` clean |

## Remaining Open Items

| # | Action | Owner | Priority |
|---|--------|-------|----------|
| 1 | Propagate `token_type` empty→`"default"` read-shape emit to other always-emit types (Okta, AppRole, AliCloud, OCI, CF, LDAP, AWS role, Azure, GCP, JWT/OIDC, Cert, Kerberos LDAP) | Amelia (Developer) | P2 — follow-up, not this retro |
| 2 | Validation matrix as a formal story-spec requirement for multi-mode types | Amelia (Developer) | P2 — carried from Epic 13 |

## Technical Debt

### Closed this session
- RADIUS omitted `tokenType` vs Vault `"default"` false-drift
- Phase B story-file / sprint-status drift (documentation-only guards from Epics 12–15)

### Explicitly not fixing
- RADIUS `resolveRADIUSPasswordKey()` ENABLE_WEBHOOKS branch — local-dev only under the webhooks-on contract

### Still open (pre-existing)
- Clear-to-default update path (cannot unset optional fields once set) — AppRole 14.1 finding, project-wide
- `token_type: ""` always-emit on older/sibling types listed above
- CF inherited `spec.username` CRD descriptions from `RootCredentialConfig` — Kubebuilder cannot override embedded field comments; docs already say use `spec.cfUsername`

## Process Issue Discovered

**Documentation-only orchestrator guards do not hold.** Epic 12 wrote atomicity as a playbook rule. Epic 13 called it not-effective. Epic 15 still drifted on 15.2. The 5-iteration review cap *did* hold because it was a visible HALT. Phase B finalization now matches that pattern: a script that exits non-zero, not another paragraph in `phase-logic.md`.

**Review-cap accept-as-is must come back to the retro.** 16.1's leftover `token_type` bug was real. Treating cap leftovers as closed work is how they survive. This retro's standing rule remains: required leftovers get patched before the retro is marked done.

## Metrics

- **Stories completed:** 5/5 (100%)
- **Total new CRDs:** RADIUS 2, AliCloud 1, OCI 2, Kerberos 3, CF 2 → 10
- **Review cap hits:** 1/5 (16.1)
- **Integration tests:** 0 stories (all Skip by policy)
- **Prep/residual items executed in-session:** 2 of 3 in-scope (tokenType + Phase B); 1 explicit no-action
- **Unit tests after RADIUS fix:** `go test ./api/v1alpha1/ -run RADIUS` pass
- **finalize-story.py tests:** 11 pass

## Next Epic Preview

**None.** Phase 2's FR coverage map ends at AE6–AE10 (Epic 16). There is no Epic 17 in `epics.md`. Lessons apply whenever the next work is planned. Open follow-up: `token_type` read-shape emit on remaining always-emit types.

## Readiness Assessment

- **Testing & quality:** Unit tests are the gate for these engines. RADIUS `token_type` comparison now uses Vault's documented read shape. `--check-epic 16` reports all five stories `done` with Code Review Records.
- **Deployment:** Not a production release epic; CRDs are additive on the epic branch.
- **Stakeholder acceptance:** Docs exist at `docs/auth-engines/{radius,alicloud,oci,kerberos,cloud-foundry}.md`.
- **Technical health:** Epic 15 TTL/credential notes held. 16.1 review-cap leftover #2 is closed. ENABLE_WEBHOOKS helper left in place by decision.
- **Unresolved blockers:** None for closing this epic. Item 4 (`token_type` propagate) is a later follow-up, not a close blocker.

## Commitments and Next Steps

1. Review this summary: `_bmad-output/implementation-artifacts/epic-16-retro-2026-08-19.md`
2. Carry the deferred `token_type` propagate item; do not start it as part of this retro
3. Next epic-dev run must use `finalize-story.py` — do not hand-edit sprint-status to `done`
